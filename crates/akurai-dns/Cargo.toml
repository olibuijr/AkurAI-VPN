[package]
name = "akurai-dns"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "AkurAI VPN internal MagicDNS: resolve *.akurai overlay names to overlay IPv4 addresses."

[lib]
name = "akurai_dns"
path = "src/lib.rs"

[[bin]]
name = "akurai-dns"
path = "src/main.rs"

# Pure-std, zero-dependency crate: the MagicDNS wire codec and UDP server link
# nothing beyond the standard library.
[dependencies]
