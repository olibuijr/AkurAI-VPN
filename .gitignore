# Rust build artifacts
/target
**/*.rs.bk

# Runtime data (never commit overlay state, keys, or peer maps)
/data/
/var/

# Local env / editor
.env
.env.*
.DS_Store
*.swp

# Agent worktrees / local tooling
.claude/
